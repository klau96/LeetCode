class Solution:
    def romanToInt(self, s: str) -> int:
        prev = ''
        total = 0
        while len(s) > 0:
            # if previous exists
            if len(prev) > 0:
                # if previous == current[0]
                
                
            # if not, then check: less or more?
            # case: less
            else:
                prev = s[0]
                s = s[1:0]

